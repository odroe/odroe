name: odroe
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ^3.6.0-334.3.beta
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  macros: ^0.1.3-main.0
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

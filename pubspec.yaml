name: odroe
description: A full-stack meta-framework for Flutter applications.
version: 0.0.0
homepage: https://odroe.dev
repository: https://github.com/odroe/odroe
issue_tracker: https://github.com/odroe/odroe/issues

environment:
  sdk: ^3.10.0

dependencies:
  analyzer: ^12.1.0
  args: ^2.7.0
  dart_style: ^3.1.8
  flutter:
    sdk: flutter
  http: ^1.5.0
  path: ^1.9.1
  roux: ^1.0.1
  web: ^1.1.1

executables:
  odroe:

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.1.0
  test: ^1.31.0

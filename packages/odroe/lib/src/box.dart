import 'package:flutter/widgets.dart';

import 'style.dart';

class Box extends StatelessWidget {
  const Box({super.key, this.style});

  final Style? style;

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}

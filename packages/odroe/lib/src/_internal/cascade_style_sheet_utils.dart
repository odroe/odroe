import '../style_sheet.dart';

extension IterableStyleSheetUtils on Iterable<StyleSheet> {
  Iterable<StyleSheet> createWith(Iterable<StyleSheet> other) {
    final result = <StyleSheet>[];
    for (final style in other) {}
  }
}
